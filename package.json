{
  "name": "coko-server-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cz": "lint-staged && ./node_modules/.bin/git-cz",
    "lint": "yarn coko-lint run --skip-stylelint",
    "release": "standard-version",
    "test": "NODE_ENV=test ALLOW_CONFIG_MUTATIONS=1 yarn jest --verbose",
    "typecheck": "yarn workspace @coko/server tsc --noEmit"
  },
  "config": {
    "commitizen": {
      "path": "cz-customizable"
    }
  },
  "devDependencies": {
    "@coko/lint": "3.0.0-alpha.7",
    "lint-staged": "^15.2.8",
    "lodash": "^4.17.21",
    "standard-version": "^7.1.0"
  },
  "packageManager": "yarn@4.9.2"
}
