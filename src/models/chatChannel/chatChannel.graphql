type ChatChannel {
  id: ID!
  created: DateTime!
  updated: DateTime!
  chatType: String!
  relatedObjectId: ID!
  messages: [ChatMessage!]!
}

type ChatChannelsResponse {
  result: [ChatChannel!]!
  totalCount: Int
}

input CreateChatChannelInput {
  chatType: String
  relatedObjectId: ID
}

extend type Query {
  chatChannel(id: ID!): ChatChannel!
  chatChannels(where: CreateChatChannelInput): ChatChannelsResponse!
}
